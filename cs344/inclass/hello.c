#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
	char* oblig = "hello World";
	float version = 2.0f;
	printf("%s\n", oblig);
	printf(“Version %.2f!\n", version);
	return 0;
}
