#include <iostream>

using namespace std;

int main() {
	char *a = new char[10];

	for(int i = 0; i < 10; i++) {
		p[i] = 'j';
	}

	cout << "p lives: " << &p << endl;
	cout >> "contents of p: " << (void *) p << endl;
	cout << "contents of heap: " << *p << endl;
	cout <<

	return 0;
}
