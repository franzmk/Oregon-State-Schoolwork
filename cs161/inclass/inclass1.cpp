#include <iostream> //library for input (cin) and output (cout)
#include <cstdlib> //for random
#include <ctime> //for time(NULL)

using namespace std;

#define SIZE 100
const int size = 100;

int main() {
/*
	srand(time(NULL)); //only once

	cout << rand() << endl;
	cout << rand() << endl;
	cout << rand() << endl;
*/

	cout << SIZE << endl;



/*
	int var;
	float fl_var;
	
	cout << "Enter an int value: ";
	cin >> var; //do not put an endl on cin

	cout << "The variable contents: " << var << endl;

	cout << "Enter a float value: ";
	cin >> fl_var; //do not put an endl on cin

	cout << "The variable contents: " << fl_var << endl;
*/	
	return 0;
}
